# Puente de Telegram: transporte puro. Deliberadamente mínimo — la API de bots de Telegram es
# HTTP plano, así que no hace falta un SDK: httpx (que ya se usa en el asistente) alcanza, y evita
# arrastrar un framework con su propio loop de eventos para cuatro llamadas.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
